uvicorn app.main:app --reload

sqlite3 caldwell_symphony.db

select * from media;
select * from users;
select * from comments;

SEED DATA  
python seed_data.py

SUPABASE URL FOR VERIFCATION ON DATA RECIEVED
https://supabase.com/dashboard/project/yxkharrshmhyfkmsbdyy/storage/files

*PY_TEST COMMANDS
# Run all tests
pytest

# Run with verbose output
pytest -v

# Run only auth tests
pytest tests/test_auth.py -v

# Run only comment tests
pytest tests/test_comments.py -v


========ALEMBIC COMMANDS=========
# 1. Make changes to your SQLAlchemy models (in app/models/models.py)

# 2. Generate a new migration
alembic revision --autogenerate -m "add user bio field"     # ← change the message

# 3. Review the generated migration file in alembic/versions/

# 4. Apply it to the database
alembic upgrade head

alembic current      # What version is the DB on?
alembic heads        # What is the latest migration?